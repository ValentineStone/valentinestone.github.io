function spamClone()
{
	var skullStripe = document.getElementById('skull-stripe');

	var skullElement = skullStripe.innerHTML;

	for (var i = 0; i < 0; i++)
	{
		skullStripe.innerHTML += skullElement;
	}
}